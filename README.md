# Processor Environment Variables

Add the following configuration to your environment file or export them before running the application:

```env
REDIS_HOST=localhost
REDIS_PORT=6379
MQTT_HOST=localhost
MQTT_PORT=8886
```
